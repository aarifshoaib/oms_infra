# Run UAT stack locally on your Mac for testing
# Usage: docker compose -f docker-compose.uat.local.yml --env-file .env.uat.local up -d

services:
  mongodb:
    image: mongo:4.4
    container_name: oms-mongodb-uat-local
    restart: unless-stopped
    # Single-node replica set so Mongoose transactions (order create, PI receive,
    # customer ledger, etc.) work. keyFile is required when auth + replSet are
    # both enabled.
    command: ["mongod", "--replSet", "rs0", "--bind_ip_all", "--keyFile", "/etc/mongo-keyfile/keyfile"]
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_ROOT_USERNAME:-admin}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_ROOT_PASSWORD}
      MONGO_INITDB_DATABASE: ${MONGO_DATABASE:-oms}
    ports:
      - "27018:27017"
    volumes:
      # Bind mount — data lives on your Mac, survives container rebuilds/down
      - ${HOME}/.oms-local/mongodb:/data/db
      - ${HOME}/.oms-local/mongo-keyfile:/etc/mongo-keyfile:ro
    networks:
      - oms-network-local
    healthcheck:
      test: ["CMD-SHELL", "mongo -u $MONGO_INITDB_ROOT_USERNAME -p $MONGO_INITDB_ROOT_PASSWORD --authenticationDatabase admin --eval \"db.adminCommand('ping')\" --quiet"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 20s

  backend:
    build:
      context: ../backend
      dockerfile: Dockerfile
      network: host
    container_name: oms-backend-uat-local
    restart: unless-stopped
    environment:
      NODE_ENV: uat
      PORT: 3000
      MONGODB_URI: ${MONGODB_URI}
      JWT_ACCESS_SECRET: ${JWT_SECRET}
      JWT_REFRESH_SECRET: ${JWT_SECRET}
      JWT_ACCESS_EXPIRES_IN: ${JWT_EXPIRATION:-24h}
      JWT_REFRESH_EXPIRES_IN: 7d
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY}
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      VAPID_PUBLIC_KEY: ${VAPID_PUBLIC_KEY}
      VAPID_PRIVATE_KEY: ${VAPID_PRIVATE_KEY}
      VAPID_EMAIL: ${VAPID_EMAIL:-admin@zyntlabs.com}
      WHATSAPP_API_VERSION: ${WHATSAPP_API_VERSION:-v21.0}
      WHATSAPP_PHONE_NUMBER_ID: ${WHATSAPP_PHONE_NUMBER_ID}
      WHATSAPP_BUSINESS_ACCOUNT_ID: ${WHATSAPP_BUSINESS_ACCOUNT_ID}
      WHATSAPP_ACCESS_TOKEN: ${WHATSAPP_ACCESS_TOKEN}
      WHATSAPP_APP_SECRET: ${WHATSAPP_APP_SECRET}
      WHATSAPP_VERIFY_TOKEN: ${WHATSAPP_VERIFY_TOKEN}
      WHATSAPP_SESSION_TTL_HOURS: ${WHATSAPP_SESSION_TTL_HOURS:-24}
      WHATSAPP_TEMPLATE_LANGUAGE: ${WHATSAPP_TEMPLATE_LANGUAGE:-en}
    ports:
      - "3000:3000"
    depends_on:
      mongodb:
        condition: service_started
    networks:
      - oms-network-local

  bff:
    build:
      context: ../bff
      dockerfile: Dockerfile
      network: host
    container_name: oms-bff-uat-local
    restart: unless-stopped
    environment:
      NODE_ENV: uat
      PORT: 3001
      BACKEND_API_URL: http://backend:3000
      JWT_SECRET: ${JWT_SECRET}
      FRONTEND_URL: http://localhost:8081
    ports:
      - "3001:3001"
    depends_on:
      - backend
    networks:
      - oms-network-local

  frontend:
    build:
      context: ../frontend
      dockerfile: Dockerfile
      network: host
      args:
        VITE_VAPID_PUBLIC_KEY: ${VAPID_PUBLIC_KEY}
    container_name: oms-frontend-uat-local
    restart: unless-stopped
    ports:
      - "8081:80"
    depends_on:
      - bff
    networks:
      - oms-network-local

networks:
  oms-network-local:
    driver: bridge
